# v0.21.0 release and showcase design

## Goal

Publish the eight contributions in PRs #25–#32 as `v0.21.0`, document the new
hover-dwell option and rendering improvements, and give community work a visible
home on both GitHub and the project website.

## Presentation

- Add a top-level `SHOWCASE` section to the README with a linked YouTube preview.
- Add a matching homepage section and docs navigation category for the Neo4j
  example, embedding YouTube's privacy-enhanced player.
- Credit [@Andy2003](https://github.com/Andy2003) prominently in the README,
  website, changelog, and GitHub Release notes for all eight release PRs.

## Release

The feature addition makes this a minor release: `0.20.0` → `0.21.0`. The
release commit, annotated tag, npm package, GitHub Release, and deployed docs
must all refer to the same version and commit.

## Validation

Run the package typecheck, full test suite, package build and dry-run pack, then
build the website. Confirm the final GitHub Actions runs before publishing the
tag, npm package, and GitHub Release.
